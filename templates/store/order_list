{% extends 'base.html' %} {% load static %}

{% block container %}

<h1>Your Orders</h1>
    {% if orders %}
        <ul>
            {% for order in orders %}
                <li>Order Date: {{ order.order_date }} | Total Price: {{ order.total_price }}</li>
                <ul>
                    {% for item in order.items.all %}
                        <li>{{ item.quantity }} x {{ item.product.name }} - {{ item.product.price }}</li>
                    {% endfor %}
                </ul>
            {% endfor %}
        </ul>
    {% else %}
        <p>You have no orders yet.</p>
    {% endif %}


{% endblock container %}